services:
  - type: web
    name: planning-gpt
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: poetry install && poetry run python backend/init_db.py && cd frontend && yarn build
    startCommand: poetry run uvicorn backend.api:app --host=0.0.0.0 --port $PORT
